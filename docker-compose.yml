version: "3.8"

services:
  ui:
    image: node:20
    working_dir: /app

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    command: sh -c "corepack enable && pnpm install && pnpm run dev"

    environment:
      - CHOKIDAR_USEPOLLING=true